dbs:
  - path: db/sqlite.db
    replicas:
      - url: s3://${S3_BUCKET}/litestream/db
        access-key-id: ${S3_ACCESS_KEY_ID}
        secret-access-key: ${S3_SECRET_ACCESS_KEY}
        region: ${S3_REGION}

exec:
  - cmd: npm run start