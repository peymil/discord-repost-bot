dbs:
  - path: db/sqlite.db
    replicas:
      - url: s3://${S3_BUCKET}/litestream/db
        endpoint: ${S3_ENDPOINT}
        access-key-id: ${S3_ACCESS_KEY_ID}
        secret-access-key: ${S3_SECRET_ACCESS_KEY}
        region: ${S3_REGION}
    snapshot-interval: 24h

exec: npm run start